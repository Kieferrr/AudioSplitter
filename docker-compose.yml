services:
  audiosplitter:
    build: .
    container_name: myaudiosplitter-container
    ports:
      - "3000:3000"
    volumes:
      - ./public:/app/public
      - ./uploads:/app/uploads
    restart: unless-stopped
